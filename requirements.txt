# Core — pinned to versions tested on the working local environment
cytools==1.4.5
numpy>=2.0
scipy>=1.12
tqdm

# CYTools native dependencies (pre-installed in staged Docker build)
# pplpy, python-flint, cysignals, gmpy2 are installed in Dockerfile stages

# Pure-Python CYTools dependencies
cvxopt
cygv==0.2.0
ortools>=9.8
osqp
qpsolvers
joblib
requests
pypalp==0.2.0
regfans==0.0.7
triangulumancer==0.2.0
platformdirs
numba

# Build tools
packaging
setuptools
wheel
